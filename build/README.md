# Build artifacts

Use `ottawa-primary-care-directory-bdp-migration-2026-08-14.zip` for the current developer handoff.

The two `1.0.0` ZIP files are deprecated custom-plugin rollback artifacts. They are retained only for history and must not be deployed as the current production solution.
