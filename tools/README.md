# Build tools

- build-business-directory-import.py generates and validates the public Business Directory Plugin migration files.
- The remaining scripts build or validate the deprecated custom-plugin rollback artifact and are not part of the current production approach.
